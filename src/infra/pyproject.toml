[project]
name = "psyflow-infra"
version = "0.1.0"
description = "PsyFlow AWS CDK infrastructure"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib==2.208.0",
    "constructs>=10.0.0,<11.0.0",
]

[tool.uv]
package = false
